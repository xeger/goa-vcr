### Testing Your Changes

To see sample generation results, gen the toy example:

```bash
goa gen github.com/xeger/goa-vcr/examples/toy/design -o examples/toy/
```

The result appears under `examples/toy/gen` which is gitignored.
